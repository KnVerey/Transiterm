<!DOCTYPE html>
<html class="no-js" lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Transiterm" %></title>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'touch-icon-ipad-retina.png', sizes: '152x152', rel: 'apple-touch-icon', type: 'image/png' %>
  </head>

  <body>
    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="left"><%= link_to (image_tag("transiterm-100px-white.png", class: "logo", alt:"Transiterm logo")), root_path, class: "logo-link" %></li>
        <li class="name"><h1><a href="<%= root_path %>"></a></h1></li>

        <li class="toggle-topbar menu-icon">
          <a href="#">
            <span>Menu</span>
          </a>
        </li>
      </ul>

      <section class="top-bar-section">
        <ul class="right">
          <li class='<%= 'active' if params[:action] == 'home' %>'><%= link_to "Home", root_path %></li>
          <li class='<%= 'active' if params[:action] == 'features' %>'><%= link_to "Features", features_path %></li>
          <li class='<%= 'active' if params[:action] == 'download' %>'><%= link_to "Download", download_path %></li>
          <li class='<%= 'active' if params[:action] == 'v2' %>'><%= link_to "Transiterm Web", v2_path %></li>
          <li class='<%= 'active' if params[:action] == 'help' %>'><%= link_to "Help", help_path %></li>
        </ul>
      </section>
    </nav>


    <div>
      <% if flash[:notice] %>
        <div data-alert class="alert-box info">
          <%= flash[:notice] %>
          <a href="#" class="close">&times;</a>
        </div>
      <% elsif flash[:alert] %>
        <div data-alert class="alert-box alert">
          <%= flash[:alert] %>
          <a href="#" class="close">&times;</a>
        </div>
      <% elsif flash[:success] %>
        <div data-alert class="alert-box success">
          <%= flash[:success] %>
          <a href="#" class="close">&times;</a>
        </div>
      <% end %>
    </div>

    <main class="row pages">
      <div class="small-12 columns">
        <%= yield %>
        <br><br><br>
      </div>
    </main>
    <div class="row footer">
      <div class="small-12 columns">
        <p>Transiterm is proud to be included in the <%= link_to "Linguistech virtual desktop", "http://linguistech.ca/discoverthevirtualdesktop", target: "_blank" %></p>
      </div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
